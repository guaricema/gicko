{{!< default}}
{{#post}}
<article>
	<div class="post">
		<header>
			<h1 class="title"><a href="{{url}}" title="{{title}}">{{title}}</a></h1>
		</header>
		<div class="post-content">
			{{content}}
		</div>
	</div>
</article>
{{/post}}